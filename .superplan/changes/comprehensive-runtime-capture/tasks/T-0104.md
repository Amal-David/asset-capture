---
task_id: T-0104
change_id: comprehensive-runtime-capture
title: widen-frame-coverage-and-verify
status: pending
priority: high
---

## Description
Extend content-script frame coverage to about:blank / srcdoc / blob: frames and verify the whole change (typecheck, tests, build).

## Acceptance Criteria
- [x] Both content scripts run in about:blank / srcdoc / blob: / data: frames (`match_about_blank` + `match_origin_as_fallback`) so embedded players and widgets in such frames are captured.
- [x] `npm test` passes.
- [x] `npm run build` (tsc --noEmit + vite build) passes.
